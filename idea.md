# Ideas
* enable video creation for manually created albums. 
* implement fallback logic for SimplePlacesResolver that does not need PostGIS extension

# refactoring
* replace if..else constructs. that is bad code style