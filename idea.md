# Ideas
* create a nicer UI. The current UI is not very user friendly. The checkbox should save the current state when clicked.
* [x] enable video creation for manually created albums. 
* [x] enable video creation for manually created albums. 
* [x] implement fallback logic for SimplePlacesResolver that does not need PostGIS extension

# Improvements - image selection for video
* [x] dont pick faces last. this should be done earlier in the process
* select images by requested orientation (landscape/portrait)


# refactoring
* replace if..else constructs. that is bad code style

# bugs
* [x] exclude screenshot images from clusters
* [ ] when changing an album name it does not reflect in the cluster names displayed in the UI and cli
